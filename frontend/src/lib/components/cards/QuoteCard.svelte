<!-- QUOTE -->
  <section
    class="mx-auto max-w-5xl px-[var(--page-padding-x)] py-24"
  >
    <div
      class="relative overflow-hidden border-t border-b border-border px-6 py-10 sm:px-10 sm:py-14"
    >
      <!-- Ambient Glow -->
      <div
        class="pointer-events-none absolute inset-0 opacity-40"
        style="
          background:
            radial-gradient(
              circle at top right,
              color-mix(
                in oklab,
                var(--color-secondary-500) 10%,
                transparent
              ),
              transparent 35%
            );
        "
      ></div>

      <div
        class="relative flex flex-col gap-8 md:flex-row md:items-start"
      >
        <!-- Index -->
        <div class="shrink-0">
          <p
            class="text-[clamp(2.5rem,6vw,5rem)] leading-none text-foreground/18"
          >
            99
          </p>
        </div>

        <!-- Quote -->
        <blockquote
          class="max-w-3xl font-display leading-[1.02] tracking-tight text-foreground"
          style="
		 font-size:
		 clamp(2rem, 5vw, 4.5rem);
		 "
        >
          “Design is not just what it looks like
          and feels like.
	  Design is how it works
          within the digital ecosystem.”
        </blockquote>
      </div>
    </div>
  </section>
